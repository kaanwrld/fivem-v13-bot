const { sunucuismi } = require("../base/settings.json");

module.exports = {
	name: 'ready',
	once: true,
	execute() {
		console.log(`Bu Bot Xhanger Network adı altında sharey tarafından kodlanmıştır.`);
	},
};
